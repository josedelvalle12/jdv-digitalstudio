# Dominio definitivo del sitio (sin barra final), usado para metadataBase,
# canonical, Open Graph, robots.txt y sitemap.xml.
# Copiá este archivo a .env.local y completá el valor real antes del deploy.
NEXT_PUBLIC_SITE_URL=https://tu-dominio.com
